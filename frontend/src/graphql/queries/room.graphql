query Room($id: String!) {
  room(id: $id) {
    id
    isPublic
    roomInfo {
      playedSeconds
      isPlaying
      playedTimestampUpdatedAt
      currentUrl
      playingIndex
      playlist {
        id
        url
        title
        author
        platform
        thumbnailUrl
      }
    }
    activeMembers {
      socketId
      roomId
      uid
    }
  }
}